interface:
  display_name: "Kujo PackWrite Workflows"
  short_description: "Compile prompts into validated agent packs"
  default_prompt: "Use $kujo-packwrite-workflows to generate, validate, or maintain PackWrite agent execution packs."
